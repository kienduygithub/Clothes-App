import { Text, View } from "react-native";
import MeStyle from "./styles/me.style";

const MeScreen = () => {
    return (
        <View style={style.container}>
            <Text>Trang chi tiết tài khoản</Text>
        </View>
    );
};

const style = MeStyle;

export default MeScreen;
