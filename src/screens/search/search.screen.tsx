import { Text, View } from "react-native";
import SearchStyle from "./styles/search.style";

const SearchScreen = () => {
    return (
        <View style={style.container}>
            <Text>Màn hình tìm kiếm</Text>
        </View>
    );
};

const style = SearchStyle;

export default SearchScreen;
